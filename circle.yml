dependencies:
  pre:
    - pip install awsebcli

deployment:
  production:
    branch: master
    commands:
      - eb use zachperkitny-prod --profile default
      - eb deploy --profile default